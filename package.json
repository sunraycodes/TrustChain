{
  "name": "trustchain",
  "version": "1.0.0",
  "description": "TrustChain - Physically-Fused, Self-Alerting Hash-Chained Ledger",
  "scripts": {
    "dev": "npx --yes concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan,green\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && node src/index.js",
    "install:all": "cd server && npm install && cd ../client && npm install"
  }
}